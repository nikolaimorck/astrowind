---
import Item from '~/components/blog/GridItem.astro';

const { posts } = Astro.props;
---

<div class="grid gap-6 row-gap-5 md:grid-cols-2 lg:grid-cols-4 -mb-6 dark:bg-slate-900 dark:text-slate-300">
	{posts.map((post) => <Item post={post} />)}
</div>
